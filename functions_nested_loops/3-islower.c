#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase
 *
 * Return: void
 */
void _islower(int c)
{
    if (c >= 'a' && c <= 'z')
    {
        _putchar(1);
    }
    else
    {
        _putchar(0);
    }
}
