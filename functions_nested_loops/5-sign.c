#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase
 *
 * Return: void
 */
int prist_sign(int n)
{
    if (n > 0)
    {
        return (1);
    }
    else if (n < 0)
    {
        return (-1);
    }
    else
    {
        return (0);
    }
}
